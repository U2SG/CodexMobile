# Public source baseline

Imported on 2026-09-13 from the local CodexMobile fork based on commit `f17143a2a63bca0adac57d7c5c4f2a0bf069d956`. The source working tree was dirty: tracked product changes and four required new frontend files were included as a snapshot, not claimed as newly authored here.

This repository starts a new Git history. Original history was not pushed because local documentation/deployment material contained private configuration. The original LICENSE is unchanged.

Excluded: environment credentials; runtime state; session logs; personal planning notes; machine-specific launch scripts; old UI screenshots; one-off local session-maintenance scripts. README, sample configuration, workflow rules, and roadmap were rewritten for publication. Real private host examples in retained source/tests were replaced with reserved example hostnames.

The original source working tree, branch, remotes, and running services remain separate. The publication scan verifies tracked-file guardrails; it does not establish that the entire application is secure or that the baseline is a tested release.
